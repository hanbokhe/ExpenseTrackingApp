<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ExpenseTrackingApp.Pages.TransactionPage"
             Title="Transactions">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Transactions"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="Start" />
            <Label Text="$4,000 | $3,000" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" FontSize="Title"/>
            <Label Text="Total:    SPEND      REMAINING" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" FontSize="Body"/>
            <Button Text="Add Transaction" Clicked="AddTransactionButton_Clicked"/>
            <Label Text="Rent" FontSize="Small" HorizontalOptions="Start"/>
            <Image  x:Name="RentImage" HeightRequest="30" WidthRequest="30" Margin="10,10,0,10" />
            
             <ListView x:Name="RentTransactionsListView"
                  Margin="2">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell Text="hello" ImageSource="Assets/Images/rent.png"/>
                       
                       
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Label Text="Entertainment" FontSize="Small" HorizontalOptions="Start"/>
            <ListView x:Name="EntertainmentTransactionListView"
                  Margin="2"
                  ItemSelected="OnListViewItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}"
                              Detail="{Binding Amount}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>