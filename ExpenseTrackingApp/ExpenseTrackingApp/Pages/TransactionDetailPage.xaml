<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="Transaction details"
             x:Class="ExpenseTrackingApp.Pages.TransactionDetailPage">
    <ContentPage.Content>
        
        <StackLayout>
            <Label Text="{Binding Name}" />
            <Label Text="{Binding Amount}"/>
            <Label Text="{Binding Month}"/>
            <Label Text="{Binding Type}"/>
            <Label Text="{Binding FileName}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Text="Back" Grid.Column="0" Clicked="BackButton_Clicked" />
                    <Button Text="Delete" BackgroundColor="red" TextColor="White" Grid.Column="1" Clicked="OnDeleteButton_Clicked" />
                </Grid>
           
        </StackLayout>
    </ContentPage.Content>
</ContentPage>